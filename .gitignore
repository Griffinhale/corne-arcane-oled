# Host test-rig build artifacts (rebuilt by sim_test/run_tests.sh)
firmware/sim_test/mechanics_runner
firmware/sim_test/visual_runner
firmware/sim_test/mechanics_hp_*
firmware/sim_test/hp_gate_*
firmware/sim_test/.noalloc.o
/.scratch/
artifacts/release/*.map
# Preserved local milestone/rollback evidence is intentionally never cleaned.
# Photo/video captures stay local until a curated set is worth publishing;
# remove the line below (or `git add -f`) to version one.
docs/evidence/
artifacts/m11-gallery/
artifacts/m11.1-baseline/
artifacts/m11.1-candidate/
artifacts/m13/*.map
*.o

# Firmware build outputs
*.uf2
*.bin
*.elf

# Nix build result symlinks
/result
/result-*

# Editor / OS noise
*~
.DS_Store

# Agent worktrees (auto-managed)
.claude/worktrees/

# Serena code-intelligence cache
.serena/

# Python tool caches
/.mypy_cache/
/.ruff_cache/
